projection;
strict ( 2 );
use draft;

define behavior for ZC_MM_SAFE_STOCK alias SafeStock
{
  use update;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}