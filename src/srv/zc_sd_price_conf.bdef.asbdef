projection;
strict ( 2 );
use draft;

define behavior for ZC_SD_PRICE_CONF alias PriceConf
{
  use create;
  use update;
  use delete;

  use action approve;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}